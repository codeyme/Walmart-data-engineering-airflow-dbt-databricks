[project]
name = "data-project-dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11,<=3.12"
dependencies = [
    "dbt-core>=1.12.2",
    "dbt-databricks>=1.10.9",
]

[project.scripts]
data-project-dbt = "data_project_dbt:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
